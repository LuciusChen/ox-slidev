#+TITLE: Research Update
#+AUTHOR: Team Ox
#+SLIDEV_THEME: seriph
#+SLIDEV_FM_DRAWINGS: false

* Opening
:PROPERTIES:
:SLIDEV_LAYOUT: cover
:SLIDEV_CLASS: px-14
:END:
Research Update

From Org source to a talk-shaped Slidev deck

Inline status [[slidev:Badge::type=warning][Internal Preview]]

* Agenda
#+begin_clicks at=2
- Problem framing
- Working assumptions
- Key result
- Implementation shape
- Feedback and rollout
- Rollout steps
- Delivery plan
#+end_clicks

#+begin_notes
Set the context before showing details.
#+end_notes

* Deck Map
:PROPERTIES:
:SLIDEV_LAYOUT: center
:END:
This deck stays mostly plain Org and only uses explicit Slidev mappings where
they improve the presentation.

#+begin_toc text-sm minDepth=1 maxDepth=2
#+end_toc

* Problem
:PROPERTIES:
:SLIDEV_LAYOUT: two-cols
:SLIDEV_FM_layoutClass: gap-12
:END:
We want a deck authoring workflow that stays readable in Org and still exports
to modern Slidev features.

- Plain Org should stay useful
- Slide-specific syntax should stay explicit
- Review output should be predictable

#+begin_right
#+begin_fragment after at=2
Current friction drops when one source can serve writing, review, and demo.
#+end_fragment
#+end_right

* Signals
The workflow is useful when the review surface gets smaller without losing
Slidev-native features.

| Signal        | Before                | After                    |
|---------------+-----------------------+--------------------------|
| Review unit   | Org + hand-edited md  | One Org source           |
| Preview loop  | Export and inspect    | Export, then Slidev HMR  |
| Final output  | Inconsistent markdown | Stable golden + demo     |

See the current reference demo at [[https://sli.dev][sli.dev]] for the target
feel, not the source format.

#+begin_notes
Pause on the review loop row. That is the real adoption win.
#+end_notes

* Workflow Map
:PROPERTIES:
:SLIDEV_LAYOUT: center
:END:
The practical loop is simple:

Org draft [[slidev:carbon:arrow-right::class=inline-block]] export
[[slidev:carbon:arrow-right::class=inline-block]] Slidev preview
[[slidev:carbon:arrow-right::class=inline-block]] review

#+begin_arrow x1=160 y1=280 x2=540 y2=280 color=#0f766e width=3 arrowSize=1
#+end_arrow

#+begin_component div class="mt-8 rounded border border-main px-4 py-3 text-sm"
One source file serves drafting, code review, and live presentation.
#+end_component

#+begin_vdrag [420,80,220,auto] class="rounded bg-white/80 px-3 py-2 text-xs shadow"
Reviewer note: markdown output remains hand-auditable.
#+end_vdrag

* Early Feedback
:PROPERTIES:
:SLIDEV_TRANSITION: fade-out
:END:
The early reaction is strongest when the authoring surface stays small.

#+begin_transform scale=0.9
#+begin_tweet id=1894208196031267235 scale=0.8
#+end_tweet
#+end_transform

#+begin_notes
Use this as a stand-in for real team feedback until we have our own examples.
#+end_notes

* Demo Clip
The deck is useful only if the preview loop is short enough to use during
editing.

#+begin_youtube id=dQw4w9WgXcQ width=640 height=360
#+end_youtube

#+begin_link href=https://sli.dev target=_blank
Slidev reference
#+end_link

* Result
:PROPERTIES:
:SLIDEV_LAYOUT: image-right
:SLIDEV_FM_image: https://cover.sli.dev
:END:
The exported deck preserves math and ordinary content.

Inline result: $f(x) = x^2 + 1$

\[
\int_0^1 x^2 dx = \frac{1}{3}
\]

The output still reads like markdown instead of generated noise.[fn:readable]

[fn:readable] This matters because exported files remain reviewable in pull requests.

* Theme Check
This deck should remain readable in either theme.

#+begin_light_or_dark
#+begin_light
Light mode keeps the explanatory slides clean and presentation-first.
#+end_light
#+begin_dark
Dark mode is useful for code-heavy or demo-heavy sections.
#+end_dark
#+end_light_or_dark

* Rollout
#+begin_clicks at=2
1. Keep most slides as plain Org.
2. Use explicit Slidev blocks only for repeated visual structure.
3. Lock output with fixture tests before polishing themes.
#+end_clicks

#+begin_notes
The point is predictable authoring, not maximum abstraction.
#+end_notes

* Example
#+ATTR_SLIDEV: :code {1|3}
#+begin_src emacs-lisp
(setq org-slidev-open-browser t)
(message "export -> preview -> review")
#+end_src

* Takeaway
- Org stays the writing format.
- Slidev stays the runtime format.
- The exporter keeps the mapping explicit and reviewable.

* Close
:PROPERTIES:
:SLIDEV_LAYOUT: quote
:END:
Readable Org first, Slidev power when it is justified.

#+begin_powered_by_slidev
#+end_powered_by_slidev
